import Editor from '../../components/Editor';
import Layout from '../../components/Layout/Layout';

const index = () => {
  return (
    <Layout>
      <Editor />
    </Layout>
  );
};

export default index;
